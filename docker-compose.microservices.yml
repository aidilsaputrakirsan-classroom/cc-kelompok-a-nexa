# ============================================================
# Docker Compose — Studyfy Microservices (Supabase Version)
# ============================================================
services:

  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    container_name: studyfy-auth-service
    restart: unless-stopped
    environment:
      DATABASE_URL: ${AUTH_DATABASE_URL}
      SECRET_KEY: ${SECRET_KEY}
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      CORS_ORIGINS: ${CORS_ORIGINS}
    ports:
      - "8001:8001"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 20s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  item-service:
    build:
      context: ./services/item-service
      dockerfile: Dockerfile
    container_name: studyfy-item-service
    restart: unless-stopped
    environment:
      DATABASE_URL: ${ITEM_DATABASE_URL}
      SECRET_KEY: ${SECRET_KEY}
      AUTH_SERVICE_URL: http://auth-service:8001
      CORS_ORIGINS: ${CORS_ORIGINS}
    ports:
      - "8002:8002"
    depends_on:
      auth-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8002/health')"]
      interval: 20s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  class-service:
    build:
      context: ./services/class-service
      dockerfile: Dockerfile
    container_name: studyfy-class-service
    restart: unless-stopped
    environment:
      DATABASE_URL: ${CLASS_DATABASE_URL}
      SECRET_KEY: ${SECRET_KEY}
      AUTH_SERVICE_URL: http://auth-service:8001
      CORS_ORIGINS: ${CORS_ORIGINS}
    ports:
      - "8003:8003"
    depends_on:
      auth-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8003/health')"]
      interval: 20s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  assignment-service:
    build:
      context: ./services/assignment-service
      dockerfile: Dockerfile
    container_name: studyfy-assignment-service
    restart: unless-stopped
    environment:
      DATABASE_URL: ${ASSIGNMENT_DATABASE_URL}
      SECRET_KEY: ${SECRET_KEY}
      AUTH_SERVICE_URL: http://auth-service:8001
      CLASS_SERVICE_URL: http://class-service:8003
      CORS_ORIGINS: ${CORS_ORIGINS}
    ports:
      - "8004:8004"
    depends_on:
      auth-service:
        condition: service_healthy
      class-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8004/health')"]
      interval: 20s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  gateway:
    image: nginx:alpine
    container_name: studyfy-gateway
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./services/gateway/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      auth-service:
        condition: service_healthy
      item-service:
        condition: service_healthy
      class-service:
        condition: service_healthy
      assignment-service:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_AUTH_API_URL: http://localhost:8001
        VITE_ITEM_API_URL: http://localhost:8002
        VITE_CLASS_API_URL: http://localhost:8003
        VITE_ASSIGNMENT_API_URL: http://localhost:8004
    container_name: studyfy-frontend
    restart: unless-stopped
    depends_on:
      gateway:
        condition: service_started

  prometheus:
    image: prom/prometheus:v2.45.0
    container_name: studyfy-prometheus
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    depends_on:
      - auth-service
      - item-service
      - class-service
      - assignment-service

  grafana:
    image: grafana/grafana:latest
    container_name: studyfy-grafana
    ports:
      - "3002:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
    volumes:
      - ./monitoring/grafana-data:/var/lib/grafana